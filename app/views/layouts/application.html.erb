<!DOCTYPE html>
<html>
  <head>
    <title>BudgetApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary text-light" style="background: royalblue">
      <div class="container-fluid">
        <%= link_to :back do %>
          <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24"><path fill="white" d="M20 11H7.83l5.59-5.59L12 4l-8 8l8 8l1.41-1.41L7.83 13H20v-2z"></path></svg>
        <%end%>
        <h1 class="fs-3 fw-normal m-0">
          <%= current_page?(new_user_session_path) ? 'LOGIN' : controller_name %>  
        </h1>
        <% if controller_name == 'sessions' %>
          <%= button_tag "Login", id: "login-submit", class: "fs-5 fw-light nav-submit", form: "login-form" %>
        <% elsif controller_name == 'registrations' %>
          <%= button_tag "Next", id: "login-submit", class: "fs-5 fw-light nav-submit", form: "signup-form" %>
        <%else%>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon">Menu</span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item d-md-flex mx-md-4">
                <a class="nav-link active" aria-current="page" href="#">Register</a>            
                <a class="nav-link" href="#">Transactions</a>
                <a class="nav-link">Payments</a>
              </li>
            </ul>
            <form class="d-flex" role="search">
              <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
          </div>
        <%end%>
      </div>
    </nav>
    <div>
      <%if notice%>
        <p class="alert alert-secondary" style="color: green"><%= notice %></p>
      <%end%>

      <%if alert%>
        <p class="alert alert-secondary"><%= alert %></p>
      <%end%>

      <%= yield %>
    </div>
  </body>
</html>
