<%= form_with(model: category, html: {id: "category-form", class: "p-4"}) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row">
    <%= form.text_field :icon, autofocus: true, placeholder: "Icon (http//..png, jpg, jpeg)", class: "form-control border-0 border-bottom mb-4" %>
  </div>

  <div class="field row">
    <%= form.text_field :name, autofocus: true, autocomplete: "name", placeholder: "Name", class: "form-control border-0 border-bottom mb-4" %>
  </div>
<% end %>