<%= form_with(model: category_purchase) do |form| %>
  <% if category_purchase.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_purchase.errors.count, "error") %> prohibited this category_purchase from being saved:</h2>

      <ul>
        <% category_purchase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :purchase_id, style: "display: block" %>
    <%= form.text_field :purchase_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
